[Unit]
After=app.service
Requires=app.service

[Service]
TimeoutStartSec=0
ExecStartPre=-/usr/bin/docker rm -f web
ExecStart=/usr/bin/docker run --name web --link app -p 80:80 web
ExecStop=/usr/bin/docker stop web

[Install]
WantedBy=multi-user.target
