[Unit]
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStartPre=-/usr/bin/docker rm -f tunnel
ExecStart=/usr/bin/docker run --name tunnel -p 22:22 tunnel
ExecStop=/usr/bin/docker stop tunnel

[Install]
WantedBy=multi-user.target
